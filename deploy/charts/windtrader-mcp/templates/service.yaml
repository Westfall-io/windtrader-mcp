apiVersion: v1
kind: Service
metadata:
  name: {{ include "windtrader-mcp.fullname" . }}
  labels:
    {{- include "windtrader-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: mcp
      protocol: TCP
      name: mcp
  selector:
    {{- include "windtrader-mcp.selectorLabels" . | nindent 4 }}
